name: "Test"
on:
  push:
    branches:
      - main

jobs:
  # Lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v3

  #     - name: Install pnpm
  #       uses: pnpm/action-setup@v2
  #       with:
  #         version: 8
  #         run_install: false

  #     - name: Install Node
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: 20
  #         cache: 'pnpm'

  #     - name: Install Dependencies
  #       run: pnpm install --no-frozen-lockfile

  #     - name: Run Lint
  #       run: npm run lint

  Test:
    runs-on: macos-latest
    # needs: Lint
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
          run_install: false

      - name: Install Node
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install Dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Setup JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: temurin

      - name: Setup Android SDK
        uses: amyu/setup-android@v3.1

      - name: Install emulator
        run: |
          sdkmanager emulator

      - name: Run tests for android 5.0
        env:
          TARGET_BROWSER: android_emulator_5
        run: |
          sdkmanager "system-images;android-21;default;x86_64"
          npm run test

      - name: Run tests for android 5.1
        env:
          TARGET_BROWSER: android_emulator_5_1
        run: |
          sdkmanager "system-images;android-22;default;x86_64"
          npm run test

      - name: Run tests for android 6
        env:
          TARGET_BROWSER: android_emulator_6
        run: |
          sdkmanager "system-images;android-23;default;x86_64"
          npm run test